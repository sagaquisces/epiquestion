{{question-tile question=model updateQuestion="updateQuestion" destroyQuestion="destroyQuestion" showNotes=true showLink=false}}

{{new-answer saveAnswer="saveAnswer" question=model}}

<h2>Answers:</h2>
<ul>
  {{#each model.answers as |answer|}}
    {{answer-tile answer=answer}}
  {{/each}}
</ul>
